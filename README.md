# DataViz Dashboard

Dashboard для визуализации данных с интеграцией PostgreSQL базы данных.

## Возможности

- 📊 Интерактивные графики (линейный, столбчатый, круговая диаграмма)
- 📋 Таблица данных с пагинацией
- 💾 Интеграция с PostgreSQL базой данных
- 🔄 RESTful API для работы с данными
- 🎨 Современный UI с Tailwind CSS

## Требования

- Python 3.8 или выше
- PostgreSQL база данных

## Установка

1. Убедитесь, что Python установлен:
```bash
python --version
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

## Настройка базы данных

1. Создайте файл `.env` в корне проекта:
```env
DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_database_name
DB_USER=your_username
DB_PASSWORD=your_password
PORT=3000
```

2. Убедитесь, что PostgreSQL запущен и база данных создана.

## Запуск

1. Запустите сервер:
```bash
python app.py
```

2. Откройте браузер и перейдите по адресу:
```
http://localhost:3000/index.html
```

## Структура проекта

```
graphics/
├── components/          # Web Components
│   ├── dataTable.js    # Компонент таблицы данных
│   ├── header.js       # Компонент заголовка
│   └── footer.js      # Компонент футера
├── app.py              # Flask сервер
├── database.py         # Модуль работы с БД
├── requirements.txt    # Python зависимости
├── .env                # Конфигурация (создайте сами)
├── index.html          # Главная страница
├── script.js           # Инициализация графиков
└── style.css           # Стили
```

## API Endpoints

### Products

- `GET /api/products?page=1&limit=10` - Получить список продуктов с пагинацией
- `GET /api/products/:id` - Получить продукт по ID
- `POST /api/products` - Создать новый продукт
- `PUT /api/products/:id` - Обновить продукт
- `DELETE /api/products/:id` - Удалить продукт

### Charts

- `GET /api/charts/line` - Данные для линейного графика (продажи по месяцам)
- `GET /api/charts/bar` - Данные для столбчатого графика (по категориям)
- `GET /api/charts/pie` - Данные для круговой диаграммы (по статусам)

## База данных

Проект использует PostgreSQL базу данных.

### Настройка подключения к PostgreSQL

1. Создайте файл `.env` в корне проекта (скопируйте `.env.example`):
```bash
cp .env.example .env
```

2. Заполните файл `.env` данными вашей PostgreSQL базы данных:
```env
DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_database_name
DB_USER=your_username
DB_PASSWORD=your_password
PORT=3000
```

3. Убедитесь, что PostgreSQL запущен и база данных создана.

### Структура таблицы products

Таблица создается автоматически при первом запуске. Структура:

```sql
CREATE TABLE products (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    category VARCHAR(100) NOT NULL,
    status VARCHAR(50) NOT NULL,
    amount DECIMAL(10, 2) NOT NULL,
    date DATE NOT NULL,
    rating INTEGER NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
```

При первом запуске база данных автоматически заполняется 50 тестовыми записями (если таблица пустая).

### Создание базы данных в PostgreSQL

Если база данных еще не создана, выполните в psql:

```sql
CREATE DATABASE your_database_name;
```

Или через командную строку:
```bash
createdb your_database_name
```

## Разработка

Flask автоматически перезагружается при изменении файлов в режиме debug (по умолчанию включен).

Для остановки сервера нажмите `Ctrl+C` в терминале.

## Лицензия

ISC


